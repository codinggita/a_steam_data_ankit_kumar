# Pull Request 1: Project Initialization

## Branch Name
`feature/project-initialization`

## Commit Message
`feat: setup project structure`

## Summary
* Initialized Vite React project inside `frontend` folder.
* Installed Material UI (`@mui/material`, `@mui/icons-material`, `@emotion/react`, `@emotion/styled`).
* Installed and configured Tailwind CSS v4 using the official `@tailwindcss/vite` plugin.
* Installed and configured Prettier and Tailwind plugins.
* Created the base feature-based folder structure inside `src/`.
* Updated `App.jsx` and `index.css` to verify Tailwind CSS & MUI styles function correctly.

## Testing
* Checked that the application builds successfully (`npm run build`).
* Verified there are no ESLint issues (`npm run lint`).
* Verified there are no compile-time or console errors.
